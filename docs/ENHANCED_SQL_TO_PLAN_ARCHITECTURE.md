# Улучшенная архитектура: SQL → План

## 🎯 **Концепция**

**Обучаем агента на SQL, используем конвертер для получения планов**

## 🏗️ **Архитектура решения**

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Пользователь  │───▶│  NL→SQL Агент   │───▶│   SQL запрос    │
│                 │    │   (Vanna AI)    │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                         │
                                                         ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Core Platform  │◀───│  SQL→План       │◀───│   SQL запрос    │
│                 │    │  Конвертер      │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 🔧 **Компоненты системы**

### **1. NL→SQL Агент (Vanna AI)**
- **Обучение**: На стандартных Q/A парах (вопрос → SQL)
- **Функция**: Генерирует SQL из естественного языка
- **Данные**: Улучшенный набор Q/A на основе данных заказчика

### **2. SQL→План Конвертер**
- **Функция**: Преобразует SQL в структурированный план
- **Формат**: JSON/XML план для Core Platform
- **Использование**: Как tool/функция в системе

### **3. Core Platform**
- **Функция**: Собирает финальный SQL из плана
- **Добавляет**: Ролевые ограничения, служебные компоненты
- **Безопасность**: RLS, проверки прав доступа

## 📊 **Преимущества подхода**

### ✅ **Для обучения:**
- **Простота** - стандартные Q/A пары
- **Эффективность** - агент хорошо работает с SQL
- **Гибкость** - легко добавлять новые примеры
- **Совместимость** - работает с существующими данными

### ✅ **Для системы:**
- **Модульность** - SQL и план разделены
- **Переиспользование** - конвертер как отдельный компонент
- **Тестируемость** - можно тестировать каждый компонент отдельно
- **Масштабируемость** - легко добавлять новые функции

## 🚀 **Реализация**

### **Этап 1: Обучение агента на SQL**
```python
# Обучаем на улучшенных Q/A парах
enhanced_examples = [
    {
        "question": "Покажи всех пользователей",
        "sql": "SELECT id, login, email FROM equsers WHERE deleted = false"
    },
    {
        "question": "Поручения за последний месяц", 
        "sql": "SELECT assignment_number, amount FROM tbl_principal_assignment WHERE assignment_date >= CURRENT_DATE - INTERVAL '1 month'"
    }
]

# Стандартное обучение Vanna AI
for example in enhanced_examples:
    vanna.train(question=example['question'], sql=example['sql'])
```

### **Этап 2: SQL→План конвертер**
```python
def sql_to_plan_converter(sql_query: str) -> Dict[str, Any]:
    """Конвертирует SQL в план для Core Platform"""
    
    # Парсим SQL
    parsed_sql = parse_sql(sql_query)
    
    # Создаем план
    plan = {
        "tables": parsed_sql.tables,
        "fields": parsed_sql.fields,
        "conditions": parsed_sql.conditions,
        "joins": parsed_sql.joins,
        "group_by": parsed_sql.group_by,
        "order_by": parsed_sql.order_by,
        "limit": parsed_sql.limit
    }
    
    return plan
```

### **Этап 3: Интеграция в систему**
```python
def process_user_query(question: str) -> str:
    """Обрабатывает запрос пользователя"""
    
    # 1. Агент генерирует SQL
    sql = vanna.generate_sql(question)
    
    # 2. Конвертируем SQL в план
    plan = sql_to_plan_converter(sql)
    
    # 3. Core Platform собирает финальный SQL
    final_sql = core_platform.build_sql_from_plan(plan)
    
    return final_sql
```

## 📋 **План реализации**

### **1. Улучшенное обучение на SQL**
- ✅ Используем улучшенный набор Q/A
- ✅ Фильтруем приоритетные таблицы
- ✅ Добавляем бизнес-контекст
- ✅ Обучаем на примерах из ТЗ

### **2. Развитие SQL→План конвертера**
- ✅ Улучшаем парсинг SQL
- ✅ Добавляем поддержку сложных запросов
- ✅ Тестируем на реальных примерах
- ✅ Оптимизируем производительность

### **3. Интеграция с Core Platform**
- ✅ API для передачи планов
- ✅ Обработка ролевых ограничений
- ✅ Валидация планов
- ✅ Обработка ошибок

## 🎯 **Следующие шаги**

1. **Запустить улучшенное обучение** на SQL
2. **Протестировать качество** генерации SQL
3. **Улучшить SQL→План конвертер**
4. **Интегрировать с Core Platform**
5. **Протестировать полный цикл**

---
**Дата создания**: 2025-01-11  
**Версия**: 1.0  
**Статус**: Готово к реализации  
**Следующий этап**: Запуск улучшенного обучения
