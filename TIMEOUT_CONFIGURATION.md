# Настройка таймаутов для генерации SQL

## Где настраиваются таймауты

### 1. В веб-интерфейсе (Simple UI - порт 3000)

В форме генерации SQL добавлено поле **"⏱️ Таймаут (сек)"**:
- По умолчанию: 500 сек для Ollama, 90 сек для GPT-4o
- Диапазон: 10-600 секунд
- Автоматически меняется при переключении провайдера

**Как использовать:**
1. Выберите провайдер (GPT-4o или Qwen/Ollama)
2. Установите нужный таймаут в поле "Таймаут (сек)"
3. Введите вопрос и нажмите "Генерировать SQL"

### 2. В Streamlit интерфейсе (порт 8501)

Добавлено поле **"⏱️ Таймаут (сек)"** в параметрах:
- По умолчанию берется из `config.env`
- Можно изменить для каждого запроса
- Диапазон: 10-600 секунд

### 3. В config.env (глобальные настройки)

```env
# Таймауты по умолчанию (в секундах)
OLLAMA_TIMEOUT=500
OPENAI_TIMEOUT=60
```

Эти значения используются, если таймаут не указан в интерфейсе.

## Рекомендуемые значения

| Провайдер | Рекомендуемый таймаут | Примечание |
|-----------|----------------------|------------|
| **GPT-4o** | 60-90 сек | Обычно генерирует за 2-5 сек |
| **Qwen 2.5-coder:1.5b** | 300-500 сек | Медленная модель, может занимать 200+ сек |
| **Qwen 3:8b** | 500-600 сек | Более мощная, но еще медленнее |

## Как работает

1. **Интерфейс** → Пользователь задает таймаут (или используется значение по умолчанию)
2. **API** → Получает таймаут из запроса или использует значение из `config.env`
3. **QueryService** → Передает таймаут в генератор SQL (SimpleOpenAISQL или OllamaNativeSQL)
4. **Генератор** → Использует таймаут для запроса к LLM

## Примеры использования

### Быстрая генерация (GPT-4o)
```
Провайдер: GPT-4o
Таймаут: 60 сек
```

### Медленная генерация (Ollama)
```
Провайдер: Qwen (Ollama)
Таймаут: 500 сек
```

### Для сложных запросов
```
Провайдер: Qwen (Ollama)
Таймаут: 600 сек (максимум)
```

## Устранение проблем

**Если таймаут слишком мал:**
- Увеличьте значение в интерфейсе
- Или измените `OLLAMA_TIMEOUT` в `config.env`

**Если таймаут слишком велик:**
- Уменьшите значение для более быстрого обнаружения проблем
- Для GPT-4o достаточно 60-90 сек

**Если таймаут не применяется:**
- Перезапустите сервисы: `./run_stack.sh restart`
- Проверьте, что значение передается в запросе (в DevTools браузера)

