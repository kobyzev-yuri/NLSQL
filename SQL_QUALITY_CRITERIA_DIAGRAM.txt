СИСТЕМА ОЦЕНКИ КАЧЕСТВА SQL (5-балльная шкала с бонусами)
================================================================

БАЗОВАЯ ОЦЕНКА: 5 баллов
├── ✅ SQL не пустой
├── ✅ Начинается с SELECT
└── ✅ Синтаксически корректный

ШТРАФЫ (вычитаются из базовой оценки):
├── ❌ Использование несуществующих таблиц (eqorders, eqpayments): -1 балл
└── ❌ Другие синтаксические ошибки: -1 балл

БОНУСЫ (добавляются к базовой оценке):
├── 🔗 JOIN операции: +1 балл
├── 📊 GROUP BY: +1 балл  
├── 🔍 WHERE условия: +1 балл
└── 🧮 Агрегатные функции (COUNT, SUM): +1 балл

СПЕЦИАЛЬНЫЕ СЛУЧАИ:
├── 📝 Простой SELECT * FROM equsers: фиксированно 3 балла
└── 🚫 Ошибки или пустой SQL: 0 баллов

ИТОГОВАЯ ФОРМУЛА:
max(0, 5 - штрафы + бонусы)

ПРИМЕРЫ ОЦЕНКИ:
===============

1. ПРОСТОЙ ЗАПРОС (3 балла):
   SELECT * FROM equsers;
   → Специальный случай = 3 балла

2. С JOIN (6 баллов):
   SELECT e.id, e.login, d.name 
   FROM equsers e 
   JOIN eq_departments d ON e.department = d.id;
   → 5 (база) - 0 (штрафы) + 1 (JOIN) = 6 баллов

3. СЛОЖНЫЙ ЗАПРОС (9 баллов):
   SELECT d.name, COUNT(u.id) AS user_count
   FROM equsers u
   JOIN eq_departments d ON u.department = d.id
   WHERE u.deleted = FALSE
   GROUP BY d.name;
   → 5 (база) - 0 (штрафы) + 1 (JOIN) + 1 (WHERE) + 1 (GROUP BY) + 1 (COUNT) = 9 баллов

4. С ОШИБКОЙ (5 баллов):
   SELECT * FROM eqorders WHERE creationdatetime >= NOW() - INTERVAL '1 month';
   → 5 (база) - 1 (eqorders не существует) + 1 (WHERE) = 5 баллов

РЕЗУЛЬТАТЫ ПО МОДЕЛЯМ:
=====================

🏆 sqlcoder:latest (7.0/5):
├── Высокие оценки: 8-9 баллов за сложные агрегации
├── Средние оценки: 6-7 баллов за JOIN'ы
└── Низкие оценки: 3 балла за простые запросы

🥈 llama3:latest (6.8/5):
├── Высокие оценки: 8-9 баллов за статистику
├── Средние оценки: 6-7 баллов за JOIN'ы  
└── Низкие оценки: 3 балла за простые запросы

🥉 qwen3:8b (6.6/5):
├── Высокие оценки: 8-9 баллов за агрегации
├── Средние оценки: 6-7 баллов за JOIN'ы
└── Низкие оценки: 3 балла за простые запросы

⚡ qwen2.5-coder:1.5b (6.4/5):
├── Высокие оценки: 8-9 баллов за сложные запросы
├── Средние оценки: 6-7 баллов за JOIN'ы
└── Низкие оценки: 3 балла за простые запросы

🐌 mistral:7b (5.8/5):
├── Высокие оценки: 9 баллов за статистику
├── Средние оценки: 6 баллов за JOIN'ы
└── Низкие оценки: 3 балла за простые запросы

ОГРАНИЧЕНИЯ СИСТЕМЫ:
===================

❌ НЕ УЧИТЫВАЕТСЯ:
├── Семантическая корректность (решает ли запрос задачу?)
├── Оптимальность выполнения
├── Сложность логики в WHERE
└── Контекстная релевантность

✅ ХОРОШО ОЦЕНИВАЕТ:
├── Структурную сложность SQL
├── Использование продвинутых возможностей
├── Синтаксическую корректность
└── Сравнительную производительность моделей

РЕКОМЕНДАЦИИ ПО УЛУЧШЕНИЮ:
==========================

1. СЕМАНТИЧЕСКАЯ ПРОВЕРКА:
   - Анализ соответствия SQL вопросу
   - Проверка логической корректности

2. ВЗВЕШЕННЫЕ БОНУСЫ:
   - Сложные JOIN'ы: до 3 баллов
   - Агрегации: до 2 баллов
   - Оптимизация: +1 балл

3. ПРОВЕРКА СУЩЕСТВОВАНИЯ:
   - Сверка с реальной схемой БД
   - Штрафы за несуществующие поля

4. ПРОИЗВОДИТЕЛЬНОСТЬ:
   - Бонус за эффективные запросы
   - Штраф за избыточную сложность










